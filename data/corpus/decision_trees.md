# Decision Trees
Decision trees recursively split on features that maximize information gain or minimize impurity (Gini, entropy).
They are interpretable and handle nonlinearities and mixed types, but single trees overfit easily.
Pruning, max depth, and min samples per leaf control complexity. Random forests and boosted trees build on this base.
Feature importance can be measured via impurity decrease or permutation importance.
