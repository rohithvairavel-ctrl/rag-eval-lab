# RAG Eval Lab — offline-first (no API keys required)
numpy>=1.26
pandas>=2.0
scikit-learn>=1.3
rank-bm25>=0.2.2
joblib>=1.3
streamlit>=1.28

# Optional denser embeddings (not required; TF-IDF used by default)
# sentence-transformers>=2.2.0
# torch>=2.0.0
